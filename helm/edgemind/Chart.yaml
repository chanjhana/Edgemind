apiVersion: v2
name: edgemind
description: >-
  EdgeMind — multi-agent AI for industrial pod-resource correlation.
  Deploys the pump-station pipeline (Layer 0) and the EdgeMind detection
  agents + correlation server (Layers 1 & 2) across the pump-station and
  monitoring namespaces.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — tracks the EdgeMind image tag family.
appVersion: "dev"
keywords:
  - edgemind
  - kubernetes
  - anomaly-detection
  - prometheus
  - industrial
home: https://github.com/
maintainers:
  - name: EdgeMind
